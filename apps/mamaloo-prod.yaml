apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mamaloo-prod
  namespace: argocd
  labels:
    app: mamaloo
    environment: prod
  annotations:
    argocd.argoproj.io/sync-wave: "10"
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: mamaloo-deployments
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    repoURL: 'https://github.com/Analarie/Portal-Mamaloo-desenvolvimento.git'
    targetRevision: main
    path: helm/mamaloo-app
    helm:
      valueFiles:
        - values-prod.yaml
      parameters:
        - name: namespace
          value: mamaloo-prod
  
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: mamaloo-prod
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
  
  revisionHistoryLimit: 10
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    - group: autoscaling
      kind: HorizontalPodAutoscaler
      jsonPointers:
        - /spec/replicas
